---
title: "Hello World - C++的基本架構"
date: 2022-06-28T20:00:46+08:00
draft: false
tags:
- CP
---
## 程式基本架構

```c++=1
#include <iostream>
using namespace std;

int main(){
	//code here
	return 0;
}
```
1.C++中的語句結尾須加上**;**。
2.以**#**開頭為程式的預處理，也就是程式編譯前執行的指令。
3.第一行為引入標頭檔，可以想像成給電腦一本參考的字典，以便認識你所使用的函式等。
`iostream`是IO(輸入輸出)與stream(流)，也就是與輸入輸出有關的函式庫。
4.第二行為使用標準命名空間，想像一下如果班上有兩個小明，那老師在點名時是不是就不知道是在叫誰？命名空間可以視為一個確定不會出現相同人名的班級，也就是一個名詞指對應到一個意義。
若不加此行，在用到此命名空間的成員時須加上`std::`，如`std::cout`。
5.`main()`為主函式，主函式程式放在`{}`中，依上到下循序執行。
6.以`//`開頭者為單行註解，註解是寫給人看的，電腦會跳過，另有多行註解，以`/*`與`*/`包裹。
7.`return 0` 使程式結束。

---
## 輸出 - 第一支程式 Hello World !

從題目給的資料大小範圍、使用的演算法等條件預估出這隻程式中將出現的最大數值大概是多少，那麼只要確保「無限大」的設置恆大於這個預估可能出現的最大數值即可。
優點大概是不用多背東西，缺點則是每次都要花時間思考又容易因為粗心或不夠了解演算法而設置不夠大的「無限大」，導致執行過程出現BUG。

---